<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bootstrap demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
  </head>
  <body>
    <%- include('navbar') %>
    <h1>Hello, customer <%= profile.name %>!</h1>

    <% if (del) { %>
        <p style="color: darkred; font-size: 10px;">Order with ID <%= del %> has been deleted</p>
    <% } %>

    <% if (orders.length > 0) { %>
      <p>your transaction with the ID :</p>
      <table class="table">
        <thead>
          <tr>
            <th scope="col">No</th>
            <th scope="col">Order Id</th>
            <th scope="col">Total</th>
            <th scope="col">Action</th>
          </tr>
        </thead>
        <tbody>
          <% orders.forEach((el, i) => { %>    
            <tr>
              <th scope="row"><%= i + 1 %></th>
              <td><%= el.orderId %></td>
              <td><%= el.total %></td>
              <td><button onclick="location.replace('/checkout/<%= el.orderId %>/delete')">delete</button></td>
            </tr>
            <% }) %>
      
        </tbody>
      </table>
      <p>needs to be paid.</p>
      <p>The total of your transaction is</p>
      <p><%= sum(orders) %></p>
      <p>click the button below to checkout</p>
      <button onclick="location.replace('/checkout-transaction')">Checkout</button>
    <% } else {%>
      <p>You dont have any transaction</p>
      <% } %>
      <button onclick="location.replace('/menus')">Browse Menu</button>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
  </body>
</html>